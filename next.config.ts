import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['drtoken.live', 'drtoken.live/storage','cdn.hellosayarwon.com'],
  },
};

export default nextConfig;
